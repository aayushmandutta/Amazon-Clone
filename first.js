let modeButton= document.querySelector("#mode");
let currMode= "light"; //stores the current modee
modeBtn.addEventListener("clicl",() =>{
    if (currMode === "light"){
        currMode = "dark";
        document.querySelector("body).")
    }else {
        currModde = "light"
    }
    console.log(currMode);
   
});